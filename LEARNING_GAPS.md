# 异步编程查漏补缺说明

## 为什么要补这几个主题

原来的 8 章已经能覆盖从协程、任务、协调、取消、队列、上下文管理器到综合项目的主线。

但如果目标是把异步编程真正学扎实，还需要补上几个“真实项目里很容易踩坑”的部分。

## 已补充的遗漏点

### 1. 并发任务失败以后怎么办

很多教程只展示任务成功的情况，但真实服务里更常见的是部分任务失败。

补充章节：

- `09_exceptions`

重点解决：

- `gather` 默认如何传播异常
- 如何用 `return_exceptions=True` 收集部分失败
- 后台任务异常为什么不能没人管
- `shield` 如何保护关键任务

### 2. 事件循环为什么会被卡住

异步代码最怕“表面用了 async，内部却写了阻塞操作”。

补充章节：

- `10_event_loop`

重点解决：

- 如何观察当前任务和存活任务
- 如何用 heartbeat 判断事件循环是否还健康
- 为什么 `time.sleep()` 会卡住所有协程
- 如何用 `asyncio.to_thread()` 做修复

### 3. 真实 I/O 通常是流式的

前面的例子主要用 `sleep` 模拟等待，但真实网络编程里常见的是一段一段传输的数据流。

补充章节：

- `11_streams`

重点解决：

- `asyncio.start_server()`
- `StreamReader`
- `StreamWriter`
- `writer.drain()` 和背压
- 连接如何优雅关闭

### 4. 异步代码怎么测试

只会写异步代码还不够，还要能验证它。

补充章节：

- `12_testing`

重点解决：

- `unittest.IsolatedAsyncioTestCase`
- 成功路径测试
- 异常路径测试
- 超时路径测试
- 测试间事件循环隔离

## 仍然刻意没有加入的内容

为了保持 `Python 3.10` 学习边界清晰，暂时没有加入这些主题：

- `asyncio.TaskGroup`：这是 `Python 3.11` 才引入的
- `asyncio.timeout()`：这是 `Python 3.11` 才引入的
- 第三方库 `aiohttp` / `httpx`：很实用，但会引入依赖，适合后续进阶
- `uvloop`：属于性能优化主题，初学阶段不急

## 下一阶段可以继续扩展什么

如果继续扩展，推荐方向是：

- 使用 `aiohttp` 或 `httpx` 写真实异步 HTTP 客户端
- 使用异步数据库客户端做真实 I/O
- 加入异步重试、退避、限流和熔断
- 加入更完整的 async 项目结构
- 对比 `Python 3.10` 和 `Python 3.11+` 的异步能力差异
